# ==========================================
# 钉钉应用凭证（远程触发功能，必需）
# ==========================================
DINGTALK_CLIENT_ID=
DINGTALK_CLIENT_SECRET=

# ==========================================
# 安全配置（必需）
# ==========================================
# 工作目录根目录限制，所有工作目录必须在此根目录下
# Windows 示例: C:\Users\yourname\Code
# macOS/Linux 示例: /Users/yourname/projects
ALLOWED_ROOT_DIR=

# ==========================================
# Web 管理界面配置（可选）
# ==========================================
# 是否启用 Web 管理界面（默认: true）
WEB_ENABLED=true

# Web 服务端口（默认: 3001）
WEB_PORT=3001

# Web 允许的跨域来源（默认: http://localhost:3001）
WEB_ALLOWED_ORIGINS=http://localhost:3001

# ==========================================
# Web 管理员认证（必需）
# ==========================================
# 管理员用户名
WEB_ADMIN_USERNAME=admin

# 管理员密码（SHA-256 哈希）
# 使用以下命令生成密码哈希:
#   npm run hash-password
# 或者使用命令行直接生成:
#   node -e "console.log(require('crypto').createHash('sha256').update('your-password').digest('hex'))"
WEB_ADMIN_PASSWORD_HASH=

# JWT 签名密钥（随机生成，保密）
# 使用以下命令生成:
#   npm run hash-password
# 或者使用命令行直接生成:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
WEB_JWT_SECRET=

# JWT Token 有效期（默认: 2h）
# 支持格式: 60, "2 days", "10h", "7d"
WEB_JWT_EXPIRES_IN=2h

# ==========================================
# 数据库配置（可选）
# ==========================================
# 数据库文件路径（默认: 项目目录下的 data/data.db）
# DB_PATH=/custom/path/data.db

# ==========================================
# Azure Foundry 配置（可选）
# ==========================================
# 如果使用 Azure Foundry 服务
# CLAUDE_CODE_USE_FOUNDRY=1
# ANTHROPIC_FOUNDRY_API_KEY=
# ANTHROPIC_FOUNDRY_RESOURCE=

# ==========================================
# LLM 配置（AI 意图识别）
# ==========================================
# LLM 提供商: volcengine | openai | anthropic | qwen | local
LLM_PROVIDER=volcengine

# 模型名称（根据 provider 不同而不同）
# volcengine: doubao-pro-32k
# openai: gpt-4o-mini, gpt-4o
# anthropic: claude-3-5-sonnet-20241022
# qwen: qwen-plus, qwen-max, qwen-turbo, deepseek-v3.2
# local: qwen2.5:7b (Ollama)
LLM_MODEL=doubao-pro-32k

# API Key
# qwen需要使用阿里云百炼的API Key (DASHSCOPE_API_KEY)
LLM_API_KEY=

# API Base URL (可选)
# volcengine: https://ark.cn-beijing.volces.com/api/v3
# qwen: https://dashscope.aliyuncs.com/compatible-mode/v1
# local: http://localhost:11434/v1 (Ollama)
LLM_BASE_URL=

# 温度参数 (默认 0.3，低温度更确定性)
LLM_TEMPERATURE=0.3

# 最大 Token 数 (默认 1024)
LLM_MAX_TOKENS=1024

# 意图识别的置信度阈值 (0-1, 默认 0.8)
# 低于此值需要用户确认
INTENT_CONFIDENCE_THRESHOLD=0.8
